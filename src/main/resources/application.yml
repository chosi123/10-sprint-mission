spring:
  application:
    name: discodeit
  profiles:
    active: dev   # 실행할 프로필 (dev / prod)

discodeit:
  storage:
    type: local
    local:
      root-path: /Users/apple/tmp/discodeit

logging:
  level:
    root: INFO